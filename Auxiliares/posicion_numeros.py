#! us/bin/ dev python
import numpy as np
import math
import matplotlib as map
import random as rn
from mapa import generate_map

def num2pos(vector, mapa):
    for i,j in mapa if (vector(i) == mapa(j)):
        







if __name__ == "__main__":
    mapa = generate_map() 
    vector = [2, 6, 8, 9]    
    

